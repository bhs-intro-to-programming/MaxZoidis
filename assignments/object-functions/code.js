const getX = (object) => {
  return object.x
}

const point = (x, y) => {
  return { x, y }
}

const emptyObject = () => {
  return {}
}

const distance = (point['x'], point['y']) => {
  return Math.sqrt(Math.abs(x ** 2) + Math.Sqrt(Math.abs(y ** 2)))
}